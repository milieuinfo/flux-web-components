import { ArgTypes, Canvas, Source } from '@storybook/blocks';
import * as VlToasterStories from './vl-toaster.stories';

# Toaster - next

<VluxMetaData id="components-next-toaster" />

Gebruik de `toaster-next` component om een toaster af te beelden.<br/>

Deze component is de nieuwe versie van het [vl-toaster](/docs/elements-toaster-h1--documentatie) element.


## Voorbeeld

```js
import { VlToasterComponent } from '@domg-wc/components/next/toaster';
```

```html
<vl-toaster-next></vl-toaster-next>
```

<Canvas of={VlToasterStories.ToasterDefault} inline />

## Configuratie

<ArgTypes of={VlToasterStories.ToasterDefault} />

## Meldingen tonen

### Dynamisch

Om een melding te tonen, gebruik je de `showAlert()` methode.

Deze methode roep je op de volgende manier op:

```ts
const toaster = document.querySelector('vl-toaster-next');
toaster.showAlert({
  type: 'error',
  title: 'Fout',
  message: 'Dit is een foutmelding'
});
```

Je kan het meegegeven object uitbreiden met de properties van de [vl-alert](/docs/components-alert--documentatie) component.

<Canvas of={VlToasterStories.ToasterShowAlert} />

### Declaratief

Daarnaast kan je de meldingen ook declaratief toevoegen in het default slot van de `vl-toaster`, typisch gebruiken we
 hiervoor de [vl-alert](/docs/components-alert--documentatie) component.

Om een melding te tonen, gebruik je de `show()` methode:

```ts
const toaster = document.querySelector('vl-toaster-next');
// toont de melding gedeclareerd in het default slot van de toaster
toaster.show();
```

<Canvas of={VlToasterStories.ToasterFadeOut} />

## Referenties

### Digitaal Vlaanderen

[Documentatie Digitaal Vlaanderen - Toaster](https://overheid.vlaanderen.be/webuniversum/v3/documentation/atoms/vl-ui-toasters)
