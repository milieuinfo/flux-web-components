import { css, CSSResult } from 'lit';

export const vlTooltipFluxStyles: CSSResult = css`
    .popover-content {
        font-size: var(--vl-font-size--xsmall);
        padding: 0.3rem 1rem;
    }
`;
        