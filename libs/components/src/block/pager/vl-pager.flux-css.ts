import { css, CSSResult } from 'lit';

export const vlPagerFluxStyles: CSSResult = css`
    [hidden] {
        display: none;
    }

    a {
        cursor: pointer;
    }

    a .vl-link__icon {
        position: relative;
    }
`;
