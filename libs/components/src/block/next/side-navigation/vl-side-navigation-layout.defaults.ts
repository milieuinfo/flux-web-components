export const sideNavigationLayoutDefaults = {
    compact: false as boolean,
    headingRootSelector: '' as string,
    contentBlock: false as boolean,
    maxDepth: undefined as number | undefined,
    navigationTitle: '' as string,
    excludeSelectors: '' as string,
    childSpacing: 'small' as string,
} as const;
