import { ArgTypes, Canvas } from '@storybook/addon-docs/blocks';
import * as VlMapSearchStories from './vl-map-search.stories';

# Map Search

<FluxComponentMetaData id="map-components-search" />


## Doel

Gebruik de `map-search` component om een zoekbalk toe te voegen aan de map waarmee gebruikers kunnen zoeken op adres.<br/>
Deze component maakt gebruik van de [select-location](/docs/map-select-location--select-location-default) component en
van de [search](/docs/components-block-search--search-default) component.


## Voorbeeld

```js
import { VlMapSearch } from '@domg-wc/map';
```

```html
<vl-map-search></vl-map-search>
```

<Canvas of={VlMapSearchStories.MapSearchDefault} />


## Configuratie

<ArgTypes of={VlMapSearchStories.MapSearchDefault} />


## Varianten

### Buiten de map

<Canvas of={VlMapSearchStories.MapSearchOutsideMap} />
