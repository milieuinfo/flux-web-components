import { ArgTypes, Canvas, Source } from '@storybook/addon-docs';
import * as VlSelectLocationStories from './vl-select-location.stories';

# Select Location - next

<VluxMetaData id="map-select-location-next" />

Gebruik de `vl-select-location-next` component om een zoekbalk af te beelden waarmee er een adres geselecteerd kan worden.

Deze component erft over van de [vl-select-rich](/docs/form-next-select-rich--documentatie) component.


## Voorbeeld

```js
import { VlSelectLocationComponent } from '@domg-wc/map';
```

```html
<vl-select-location-next></vl-select-location-next>
```

<Canvas of={VlSelectLocationStories.SelectLocationDefault} />


## Configuratie

<ArgTypes of={VlSelectLocationStories.SelectLocationDefault} />


