import { ArgTypes, Canvas } from '@storybook/addon-docs/blocks';
import * as VlMapLayerStyleStories from './vl-map-layer-style.stories';

# Map Layer Style

<FluxComponentMetaData id="map-components-layer-style-layer-style" />


## Doel

Gebruik de `map-layer-style` component om een
[features-layer](/docs/map-layer-vector-layer-features-layer--map-features-layer-default) of een
[wfs-layer](/docs/map-layer-vector-layer-wfs-layer--map-wfs-layer-default) te stylen.<br/>
**Let op**: Bij gebruik van de [map-legend](/docs/map-legend--map-legend-features-layer-multiple-styles) worden de
attributen niet reactief toegepast op de legende.


## Voorbeeld

```js
import { VlMapLayerStyle } from '@domg-wc/map';
```

```html
<vl-map-layer-style></vl-map-layer-style>
```

<Canvas of={VlMapLayerStyleStories.MapLayerStyleDefault} />


## Configuratie

<ArgTypes of={VlMapLayerStyleStories.MapLayerStyleDefault} />


## Varianten

### Met tekst

<Canvas of={VlMapLayerStyleStories.MapLayerStyleText} />

### Met legende

<Canvas of={VlMapLayerStyleStories.MapLayerStyleLegend} />
