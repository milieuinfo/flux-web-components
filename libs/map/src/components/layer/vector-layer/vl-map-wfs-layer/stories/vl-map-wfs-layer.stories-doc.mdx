import { ArgTypes, Canvas } from '@storybook/addon-docs/blocks';
import * as VlMapWfsLayerStories from './vl-map-wfs-layer.stories';

# Map WFS Layer

<FluxComponentMetaData id="map-components-layer-vector-wfs-layer" />


## Doel

Gebruik de `map-wfs-layer` component om een WFS kaartlaag af te beelden.<br/>
Deze component erft over van de `VlMapVectorLayer` klasse, die op zijn beurt overerft van de `VlMapLayer` klasse.


## Voorbeeld

```js
import { VlMapWfsLayer } from '@domg-wc/map';
```

```html
<vl-map-wfs-layer></vl-map-wfs-layer>
```

<Canvas of={VlMapWfsLayerStories.MapWfsLayerDefault} />


## Configuratie

<ArgTypes of={VlMapWfsLayerStories.MapWfsLayerDefault} />
