import { css, CSSResult } from 'lit';

const styles: CSSResult = css`
    :host {
        display: block;
    }

    :host label {
        display: block;
    }

    :host [layer] {
        display: block;
    }
`;
export default styles;
