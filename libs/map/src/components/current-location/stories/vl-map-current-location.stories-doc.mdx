import { ArgTypes, Canvas } from '@storybook/addon-docs/blocks';
import * as VlMapCurrentLocationStories from './vl-map-current-location.stories';

# Map Current Location

<FluxComponentMetaData id="map-components-current-location" />


## Doel

Gebruik het `map-current-location` component om een huidige locatie knop af te beelden op de map.<br/>
Na het drukken op de knop wordt de huidige locatie van de gebruiker centraal afgebeeld.


## Voorbeeld

```js
import { VlMapCurrentLocation } from '@domg-wc/map';
```

```html
<vl-map-current-location></vl-map-current-location>
```

<Canvas of={VlMapCurrentLocationStories.MapCurrentLocationDefault} />


## Configuratie

<ArgTypes of={VlMapCurrentLocationStories.MapCurrentLocationDefault} />
